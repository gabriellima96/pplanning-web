// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyBCrKpvmAmKSwbX6Wohlxpktg-vfT41jFs',
    authDomain: 'pplanning-8eff0.firebaseapp.com',
    databaseURL: 'https://pplanning-8eff0.firebaseio.com',
    projectId: 'pplanning-8eff0',
    storageBucket: 'pplanning-8eff0.appspot.com',
    messagingSenderId: '880533166165',
    appId: '1:880533166165:web:fd4ce77c4cc5c245349cdc'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
